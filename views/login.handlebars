<h1>Login</h1>
<form method="POST" class="mostForms">
{{#if somethingwrong}}
<h3>Please provide a valid e-mail and password</h3>
{{/if}}
    <input type="text" name="email" placeholder="e-mail address" autocomplete="off">
    <input type="text" name="password" placeholder="password" autocomplete="off">
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    <button type='submit'>Login</button>
    <h4 >Haven't registered yet? Go <a href="/register" style="color:tomato;">here</a> !</h4>
</form>
